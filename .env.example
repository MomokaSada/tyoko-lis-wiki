DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

### Application FrontEnd
NEXT_PUBLIC_APP_URL=

### Application BackEnd
NEXT_PUBLIC_RP_ID=
NEXT_PUBLIC_RP_ORIGIN=
APP_SESSION_COOKIE_NAME=

### Development (override for local dev machine hostname)
# NOTE: .local ホスト名を設定すると HMR WebSocket (wss://) が失敗し、
# ページが定期的にリロードされる問題があったため、現在は使用していない。
# モバイル確認が必要な場合は LAN IP で直接アクセスしてください。
# NEXT_PUBLIC_DEV_HOSTNAME=